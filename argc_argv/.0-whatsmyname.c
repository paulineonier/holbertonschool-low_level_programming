#include <stdio.h>

/**
 * main - Entry point
 * @argc: arguments
 * @argv: array
 * Return: 0
 */

int main (int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
