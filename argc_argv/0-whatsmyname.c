#include <stdio.h>

/**
 * main - Entry point
 * @argc: array who tell me how much arguments
 * @argv: arguments
 * Return: 0 on success
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
